Program patito;
vars
  i, j, k, c: int;
  f: float;
  s: string;

void function log()
{
  print("Dentro de la funcion uno");
}

int function sum(x: int)
vars
  res: int;
{
  res = x + i;
  return(res);
}

main ()
{
  i = 10;
  println("Operacion = ", sum(i / 2));
}
